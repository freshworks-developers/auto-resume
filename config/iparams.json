{
  "apikey": {
    "display_name": "Freshdesk API Key",
    "description": "Agent API key from Profile Settings → View API Key. Enter the key only (the app appends :X for Basic auth).",
    "type": "text",
    "required": true,
    "secure": true
  }
}
